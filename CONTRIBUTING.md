# 贡献指南

欢迎补充文档、代码示例、练习题和调试经验。这个资料库面向通信工程、电子信息、自动化等专业的嵌入式初学者，所有内容都要尽量讲清楚“概念如何变成工程能力”。

## 写作风格

- 使用中文，像老师讲课一样写给初学者。
- 不要写成论文，也不要只堆定义。
- 每篇文档尽量包含：本节要解决什么问题、基本概念、工程用途、代码或伪代码、常见错误、调试方法、面试问答、简历表达、小练习和小结。
- 尽量结合 RS485、Modbus、FreeRTOS、Qt 上位机、工业控制、机器视觉控制系统等抽象场景。

## 目录和命名规范

- 顶层目录按学习阶段编号，例如 `01_单片机基础`、`03_RS485_Modbus`、`07_Qt上位机`。
- 文档文件名尽量使用“序号_主题.md”，例如 `04_PWM输出.md`、`06_Modbus_RTU帧格式.md`。
- 示例代码统一放在对应模块的 `codes` 或项目子目录中，不要散落在仓库根目录。
- 新增文档后，同步检查是否需要更新 `DOCS_INDEX.md`、`README.md`、`CHANGELOG.md` 和模块内 README。
- 如果只是补充一小段内容，优先在原文档中追加，不要另开一个名字相近的新文件。

## 内容边界

- 不得泄露真实公司、客户、产线、设备内部资料或未公开项目细节。
- 不要编造项目成果，不要把学习项目写成真实交付项目。
- 涉及硬件接线、端子定义、控制卡、驱动器、传感器时，必须提醒读者以数据手册、端子定义、原理图和实测结果为准。
- 不要编造具体设备寄存器地址。需要举例时，请明确说明是教学模拟地址或模拟数组索引。

## 代码要求

- C 语言示例尽量偏嵌入式风格，避免依赖具体芯片库。
- 示例代码要短小、可读、注释清楚。
- C++ 示例代码不要写 `std::` 前缀。
- 如果代码只是伪代码，请明确说明它用于教学流程，不代表真实设备协议。

## 示例代码要求

| 类型 | 推荐写法 | 不建议写法 |
| --- | --- | --- |
| C 语言 | 用数组、结构体、状态机、回调占位函数解释流程 | 依赖某个具体芯片库后才能看懂 |
| Modbus/RS485 | 使用模拟寄存器、模拟 TX/RX、教学地址 | 写成真实设备私有协议或真实寄存器表 |
| FreeRTOS | 说明任务、队列、信号量之间的关系 | 只贴 API 调用，不解释任务边界 |
| C++ | 用类封装协议、设备、状态机概念 | 展示过度复杂的模板或动态分配技巧 |
| Linux/Qt | 给出串口、Socket、日志、界面交互的最小闭环 | 把上位机写成不可运行的大段伪代码 |

示例代码要优先服务教学目的：读者应当能看出“数据从哪里来、在哪里缓存、在哪里解析、在哪里输出日志”。如果无法编译运行，需要在文件开头明确说明原因。

## 提交流程建议

1. 先确认新增内容放在哪个目录。
2. 按已有文档结构补充内容。
3. 搜索是否误写真实公司、客户、产线或具体设备内部信息。
4. 搜索 `std::`，确认 C++ 示例符合本仓库约定。
5. 如果新增文档，更新 `DOCS_INDEX.md` 和必要的模块 README。
6. 如果修改了示例代码，尽量运行对应目录的 `make` 或语法检查命令。
7. 更新 `CHANGELOG.md`，把新增、更新和边界变化写清楚。

## 提交前检查

建议至少执行下面几类检查：

```text
git diff --check
rg -n "真实公司|客户|产线|内部资料" .
rg -n "std::|using namespace std" .
```

说明：

- `git diff --check` 用于发现行尾空格和补丁格式问题。
- 如果敏感词只出现在“不得泄露”的规则说明中，可以忽略；如果出现在项目案例或简历表达中，要重新检查是否越界。
- `std::` 命中规则说明文本可以忽略，但 C++ 示例代码中不应出现该前缀。
- 修改 `03_RS485_Modbus/codes`、`04_FreeRTOS/codes`、`06_嵌入式Linux/codes` 时，优先运行对应目录的 `make`。

## 推荐贡献方向

- 补充项目实战中的测试用例和调试日志样例。
- 补充 FreeRTOS、Qt、Linux 串口编程的面试追问。
- 把复杂知识点改写成更适合初学者理解的讲义。
- 增加公开、可复现、无真实项目泄露风险的教学示例。
